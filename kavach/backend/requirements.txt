fastapi>=0.115,<1.0
uvicorn[standard]>=0.30,<1.0
pydantic>=2.9,<3.0
python-dotenv>=1.0,<2.0

# Phase 1 — Repository ingestion + RAG
# Exact versions below were confirmed working in this project's dev
# environment (see terminal install logs during setup) — pinned with ==
# rather than >= since these are large, fast-moving ML libraries where a
# future major version is more likely to break the embedding pipeline.
qdrant-client==1.19.0
sentence-transformers==6.0.0

# Testing
pytest>=8.0,<9.0
pytest-cov>=5.0,<6.0
httpx>=0.27,<1.0

# Add as later phases need them:
# sqlalchemy           # Phase 1 (DB) — audit/workflow storage
